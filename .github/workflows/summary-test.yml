name: SummaryTest

on:
  workflow_dispatch:

jobs:
  # Determine if build should run based on changed files
  changes:
    runs-on: ubuntu-latest
    steps:
      # Fast path: push to main always builds - no checkout needed
      - name: HW
        run: echo "# Hello world" >> $GITHUB_STEP_SUMMARY
